import 'package:flutter/material.dart';

class CookingTips extends StatelessWidget {
  const CookingTips({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FAVORITE'),
      ),
      body: Container(),
    );
  }
}
